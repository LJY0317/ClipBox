# Desktop app

The desktop GUI will be added after the shared core and CLI contracts are stable.

The intended direction is a Tauri-based desktop application that calls the same core APIs as the `clipbox` CLI rather than using the CLI as its internal engine.
